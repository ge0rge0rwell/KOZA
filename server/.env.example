# ==============================================
# KOZA Node.js Backend — Environment Variables
# ==============================================
# Copy this file to .env and fill in the values.
# NEVER commit the .env file to source control.

# --- Server ---
PORT=4000
KOZA_FRONTEND_URL=http://localhost:5173

# --- Database (PostgreSQL) ---
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/koza_db

# --- Redis (BullMQ) ---
REDIS_URL=redis://127.0.0.1:6379

# --- Token Security ---
# Must be EXACTLY 32 characters (256-bit key for AES-256-GCM)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex').slice(0, 32))"
ENCRYPTION_KEY=your_32_char_encryption_key_here!

# --- Mastodon OAuth App Credentials ---
# Create an application in your Mastodon instance:
# Settings -> Development -> New Application
MASTODON_INSTANCE_URL=http://localhost:3000
MASTODON_CLIENT_ID=your_mastodon_client_id
MASTODON_CLIENT_SECRET=your_mastodon_client_secret
MASTODON_REDIRECT_URI=http://localhost:4000/api/oauth/mastodon/callback
